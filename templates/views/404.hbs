<!DOCTYPE html>
<html>
    <head>
        <title>404 Page</title>

        {{>head}}

        <link href="css/style.css" rel="stylesheet" type="text/css">
        
    </head>

    <body>
        <div>
            {{>nav}}

            {{>header}}
            
            <h1>{{title}}</h1>
            <p>{{errorMessage}}</p>
            {{>footer}}
        </div>
    </body>
</html>